const express = require("express");
const router = express.Router();

/**
 * Email verification routes are disabled.
 * This file is left intentionally minimal in case
 * future email features are reintroduced.
 */

console.log("[INFO] Email verification routes are disabled.");

module.exports = router;
